import React from 'react';

const WaterBackground: React.FC = () => {
  return (
    <div className="fixed inset-0 z-0 bg-blue-500 opacity-50">
      {/* Placeholder for water background animation or image */}
    </div>
  );
};

export default WaterBackground;